$('#posts').append("<%= j render(@posts) %>");

<% if @posts.next_page %>
    $('.pagination').replaceWith("<%= j  will_paginate @clients, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>");
<% else %>
    $('.pagination').remove();
<% end %>
